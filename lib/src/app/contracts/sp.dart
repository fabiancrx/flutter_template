import 'dart:convert';

class Mapper {
  // static mapList<T>(String source) {
  //   final jsonList = jsonDecode(source) as List;
  //   final items = jsonList.map((i) => instance.fromJson(i)).cast<T>().toList();
  //   return items;
  // }
  //
  // static map<T>(String data) {
  //   final json= jsonDecode(data) ;
  //   if (data is Map || (data is List && data.isNotEmpty)) {
  //     final item = instance.fromJson(data?.isNotEmpty ?? false ? json.first : null);
  //   } else {
  //     throw ('Response is not an instance of Map<String, dynamic> :${data.runtimeType}');
  //   }
  // }
}
